import { DatacontentDirective } from './datacontent.directive';

describe('DatacontentDirective', () => {
  it('should create an instance', () => {
    const directive = new DatacontentDirective();
    expect(directive).toBeTruthy();
  });
});
