import { IndexScrollDirective } from './index-scroll.directive';

describe('IndexScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexScrollDirective();
    expect(directive).toBeTruthy();
  });
});
