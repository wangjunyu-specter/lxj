import { XclistattrPipe } from './xclistattr.pipe';

describe('XclistattrPipe', () => {
  it('create an instance', () => {
    const pipe = new XclistattrPipe();
    expect(pipe).toBeTruthy();
  });
});
