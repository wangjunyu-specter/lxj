import { ModelTimePipe } from './model-time.pipe';

describe('ModelTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ModelTimePipe();
    expect(pipe).toBeTruthy();
  });
});
