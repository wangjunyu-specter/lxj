import { NewslisttimePipe } from './newslisttime.pipe';

describe('NewslisttimePipe', () => {
  it('create an instance', () => {
    const pipe = new NewslisttimePipe();
    expect(pipe).toBeTruthy();
  });
});
