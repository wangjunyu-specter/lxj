import { DatecontentPipe } from './datecontent.pipe';

describe('DatecontentPipe', () => {
  it('create an instance', () => {
    const pipe = new DatecontentPipe();
    expect(pipe).toBeTruthy();
  });
});
