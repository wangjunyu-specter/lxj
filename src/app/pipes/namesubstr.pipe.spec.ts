import { NamesubstrPipe } from './namesubstr.pipe';

describe('NamesubstrPipe', () => {
  it('create an instance', () => {
    const pipe = new NamesubstrPipe();
    expect(pipe).toBeTruthy();
  });
});
