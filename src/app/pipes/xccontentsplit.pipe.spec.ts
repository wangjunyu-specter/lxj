import { XccontentsplitPipe } from './xccontentsplit.pipe';

describe('XccontentsplitPipe', () => {
  it('create an instance', () => {
    const pipe = new XccontentsplitPipe();
    expect(pipe).toBeTruthy();
  });
});
