import { CarorotherPipe } from './carorother.pipe';

describe('CarorotherPipe', () => {
  it('create an instance', () => {
    const pipe = new CarorotherPipe();
    expect(pipe).toBeTruthy();
  });
});
